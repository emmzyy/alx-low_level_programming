#include "main.h"

/**
 * print_sign - prints the sign of a number
 * @n: the letter condition fr returning values
 * Return: 0 and 1
 */
int print_sign(int n)
{
	int n;

	if (n > 0)

		return (1);
		_putchar('+);

	else if (n = 0)

		return(0)
		_putchar('0');

	else

		return (-1);
		_putchar(' - ');


}
