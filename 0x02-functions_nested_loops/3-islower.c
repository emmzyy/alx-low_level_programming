#include "main.h"

/**
 * _islower - checks for lowercase c 
 * Return: 1 or 0
 */
int _islower(int c);

{
char letters;
for (letters = 'a'; letters <= 'z'; letters++)

	{
		if (letters = 'c');
			return (1);
		else
			return (0);
	}
}
